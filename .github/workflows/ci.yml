name: CI

on:
  push:
    branches: [master, main, "cursor/**"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install "ruff>=0.6"

      - name: Ruff check
        run: ruff check src tests

      - name: Ruff format check
        run: ruff format --check src tests

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package (dev + optional adapters)
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,aws,vault]"

      - name: Run tests
        run: pytest -q -m "not e2e_live" --junitxml=junit-${{ matrix.python-version }}.xml

      - name: Upload junit
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: junit-py${{ matrix.python-version }}
          path: junit-${{ matrix.python-version }}.xml
          if-no-files-found: ignore

  build:
    name: Build sdist/wheel
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build distributions
        run: python -m build

      - name: Check metadata
        run: twine check dist/*

      - name: Smoke-install wheel
        run: |
          pip install dist/*.whl
          secret-broker version
          secret-broker harness list
          python -c "from secret_broker.harness.assets import list_artifact_ids; ids=list_artifact_ids(); assert ids, ids"

      - uses: actions/upload-artifact@v7
        with:
          name: python-package-distributions
          path: dist/

  plugins:
    name: Validate & package harness plugins
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Artifact + install wiring tests
        run: pytest -q tests/test_plugin_artifacts.py tests/test_harness_artifact_install.py

      - name: Build plugin release zips
        run: secret-broker harness package --dest dist/plugins

      - uses: actions/upload-artifact@v7
        with:
          name: harness-plugin-zips
          path: dist/plugins/*.zip
