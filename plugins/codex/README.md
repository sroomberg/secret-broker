# Codex plugin artifact

## Install (today)

```bash
secret-broker harness install codex --scope project --hooks
```

## Breakout

Publish `mcp.fragment.toml` + `hooks/` as a Codex plugin package. Core keeps MCP server only.
